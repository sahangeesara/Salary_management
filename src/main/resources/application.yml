spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hr_managment?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: admin@123
    driver-class-name: com.mysql.cj.jdbc.Driver

#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          secret-key: my-super-secret-key-which-is-long


#app:
#  jwt:
#    secret: bAGUpSTYQn1zDE03wyAC4v1iqqcGKdIexDoW4vo6WAp80gKcEMHTmKr9ya31fGFW
#    expiration: 86400000


## Database Connection
## Added ?currentSchema=public to ensure your app stays in the right lane
#  spring.datasource.url=jdbc:postgresql://db.nzsmvdrhedvmoxdpxgtm.supabase.co:5432/postgres?user=postgres&password=admin@123@99&currentSchema=public
#  spring.datasource.username=postgres
#  spring.datasource.password=admin@123@99
#  spring.datasource.driver-class-name=org.postgresql.Driver
#
#  # Connection Pool Settings
#  spring.datasource.hikari.maximum-pool-size=10
#  spring.datasource.hikari.minimum-idle=5
#
#  # Flyway configuration
#  spring.flyway.enabled=true
#  # This ensures Flyway only looks at your public tables
#  spring.flyway.schemas=public
#  # If your database already has tables (which we created in the Supabase editor),
#  # set this to true so Flyway doesn't crash on the first run
#  spring.flyway.baseline-on-migrate=true
